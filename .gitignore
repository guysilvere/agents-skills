.DS_Store
*.pyc
__pycache__/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Secrets d'application — le dépôt est PUBLIC : jamais de .env versionné
.env
.env.*
!.env.example

# Fichiers de config locaux générés (jamais versionnés — propres à chaque machine)
.claude/
.gemini/
.ignore
.mcp.json
AGENTS.md
GEMINI.md
mcp-servers.md
opencode.json
